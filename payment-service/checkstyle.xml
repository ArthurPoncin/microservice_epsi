<?xml version="1.0"?>
<!DOCTYPE module PUBLIC
    "-//Checkstyle//DTD Checkstyle Configuration 1.3//EN"
    "https://checkstyle.org/dtds/configuration_1_3.dtd">

<module name="Checker">
    <property name="charset" value="UTF-8"/>
    <property name="fileExtensions" value="java"/>

    <!-- Longueur de ligne max 150 caractères (on ignore les imports et packages) -->
    <module name="LineLength">
        <property name="max" value="150"/>
        <property name="ignorePattern" value="^package.*|^import.*"/>
    </module>

    <module name="TreeWalker">

        <!-- ===== Imports ===== -->
        <module name="UnusedImports"/>
        <module name="RedundantImport"/>

        <!-- ===== Nommage ===== -->
        <!-- Types (classes, interfaces, enums) : PascalCase -->
        <module name="TypeName"/>
        <!-- Méthodes : camelCase -->
        <module name="MethodName"/>
        <!-- Constantes : UPPER_SNAKE_CASE -->
        <module name="ConstantName"/>
        <!-- Variables locales : camelCase -->
        <module name="LocalVariableName"/>
        <!-- Paramètres de méthodes : camelCase -->
        <module name="ParameterName"/>
        <!-- Champs d'instance : camelCase -->
        <module name="MemberName"/>

        <!-- ===== Blocs et accolades ===== -->
        <module name="NeedBraces"/>
        <module name="EmptyBlock">
            <property name="option" value="text"/>
        </module>
        <module name="LeftCurly"/>
        <module name="RightCurly"/>

        <!-- ===== Espaces ===== -->
        <module name="WhitespaceAfter"/>
        <module name="NoWhitespaceBefore"/>
        <module name="GenericWhitespace"/>

        <!-- ===== Qualité du code ===== -->
        <!-- Pas de catch vide sans commentaire -->
        <module name="EmptyCatchBlock">
            <property name="exceptionVariableName" value="expected|ignore|ignored"/>
        </module>
        <!-- Pas de modificateurs redondants (ex: public sur interface) -->
        <module name="RedundantModifier"/>
        <!-- Éviter les blocs de code inutiles -->
        <module name="AvoidNestedBlocks"/>

    </module>
</module>
